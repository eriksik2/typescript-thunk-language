{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build:editor",
      "type": "shell",
      "command": "bun run build:editor",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
