# Generated Thunk compile output

Files here are produced by:

```bash
bun scripts/compile.ts examples/<name>.thunk
bun scripts/compile.ts examples/<name>.thunk --run
```

`control-flow.compiled.ts` is the lowered TypeScript for the control-flow showcase.
